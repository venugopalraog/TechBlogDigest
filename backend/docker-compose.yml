services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: engblog
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - engblog-postgres-data:/var/lib/postgresql/data

volumes:
  engblog-postgres-data:
